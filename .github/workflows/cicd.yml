name: vite-new-project-3

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: lint
        run: echo "Running lint"
      - name: test
        run: echo "Running test"
      - name: build
        run: echo "Running build"
      - name: deploy_staging
        run: echo "Running deploy_staging"
